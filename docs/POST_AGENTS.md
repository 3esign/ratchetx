# Machines-welcome post (for the timeline)

Announces the agents update: the MCP server, the x402 door, and the category said
out loud. Post after the site deploy is verified. Numbers as of 2026-08-26 —
re-check /api/game?action=arena before posting.

---

## Single post

Machines can now play RATCHET.

A zero-dependency MCP server puts the whole arcade inside any AI client — Claude,
or anything that speaks MCP. Read the board, fire sealed commit-reveal shots
settled on Pyth prices, and wear a public, oracle-settled accuracy record that
cannot be edited afterwards. Wins AND losses. Demo mode needs no wallet at all:

github.com/3esign/ratchetx/blob/main/mcp/README.md

And a second door, shipped dark until we arm it: x402. An agent wallet that never
touched $RCX will be able to enter the arena by paying a small USDC toll — paid
DIRECTLY to the current daily champion. A player, never us. Verified on-chain,
never custodied, 0% to the team, like everything else here:

github.com/3esign/ratchetx/blob/main/docs/X402.md

Because this is what we are: a prediction market arcade with no house. Order books
have spreads, custodians and committees. RATCHET has an oracle, a burn, and a
proof page — and now it has room for your agent. The four house bots are waiting
to be beaten: ratchetx.xyz

---

## Thread version (if you'd rather thread it)

**1/** Machines can now play RATCHET. A zero-dependency MCP server puts the whole
arcade inside any AI client — read the board, fire sealed shots settled on Pyth,
wear a public record nobody can edit afterwards. Demo mode needs no wallet. 🧵

**2/** There is no special AI path — that's the point. Agents fire through the
identical signed API a person uses: same oracle, same sealing, same settlement
rule, same leaderboard. The four house bots lose in public and are there to be
beaten. github.com/3esign/ratchetx/blob/main/mcp/README.md

**3/** Second door, shipped dark until armed: x402. An agent wallet that never
touched $RCX pays a small USDC toll to enter the arena — paid DIRECTLY to the
current daily champion. A player, never us. On-chain verified, replay-gated,
never custodied. github.com/3esign/ratchetx/blob/main/docs/X402.md

**4/** Say the category out loud: RATCHET is a prediction market arcade with no
house. Order books have spreads, custodians and committees. We have an oracle, a
burn, a proof page — and a program that goes immutable on Sept 8, registered in
advance. ratchetx.xyz
