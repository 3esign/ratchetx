# Machines-welcome post (for the timeline)

Follow-up for the agent update: the zero-install remote MCP, the measurable record,
and the category said out loud. Post after the site deploy is verified. Numbers —
re-check /api/game?action=arena before posting.

---

## Single post

Machines can now play RatchetX.

A public Streamable HTTP MCP endpoint now puts the free arcade demo inside any
compatible client by URL — no clone, package, account, or wallet. Read the board,
fire sealed commit-reveal probability calls settled on Pyth prices, and build a
public Brier record that cannot be edited afterwards. Wins AND losses:

ratchetx.xyz/api/mcp

Ranked mode still keeps the signer local. The second door now speaks standard x402
v2 exact SVM through a facilitator, but stays OFF until one funded mainnet smoke
proves the whole settlement. Its toll pays the quoted current champion — a player,
never us — and Ratchet is not claiming a Bazaar listing early:

github.com/3esign/ratchetx/blob/main/docs/X402.md

Because this is what we are: a prediction market arcade with no house. Order books
have spreads, custodians and committees. RatchetX has an oracle, a burn, and a
proof page — and now it has room for your agent. The four house bots are waiting
to be beaten: ratchetx.xyz

---

## Thread version (if you'd rather thread it)

**1/** Machines can now play RatchetX without installing it. Connect a compatible
MCP client to ratchetx.xyz/api/mcp, read the board, and fire free sealed probability
calls settled on Pyth. No clone, package, account, wallet, or token. 🧵

**2/** There is no special AI path — that's the point. Agents fire through the
identical signed API a person uses: same oracle, same sealing, same settlement
rule, same leaderboard. The four house bots lose in public and are there to be
beaten. github.com/3esign/ratchetx/blob/main/mcp/README.md

**3/** Ranked signing stays local. Standard x402 v2 SVM is built and tested, but
stays OFF until a funded mainnet payment proves facilitator settlement and retry
recovery end to end. Bazaar discovery comes after arming, not before it.
github.com/3esign/ratchetx/blob/main/docs/X402.md

**4/** Say the category out loud: RatchetX is a prediction market arcade with no
house. Order books have spreads, custodians and committees. We have an oracle, a
burn, a proof page — and a program that goes immutable on Sept 8, registered in
advance. ratchetx.xyz
