Ownership amendment: Sol/native Windows owns Cargo/SBF; Fable released it in COWORK_HANDOFF section0. Earlier snapshot owner wording below is superseded.

# RatchetX server-off review — 2026-09-05

Read-only audit for the existing PERMANENCE_EXECUTION_PLAN.md; this is evidence for that tracker, not a second roadmap. Laptop project root: D:/Work/Software_Projects/pumpmind/ratchetx/ratchet_phase_a_clean. Fable owns build/integration; Antigravity owns the lifecycle/inspector edits. Findings describe files read during this audit, before their ongoing fixes are acknowledged.

## Immediate release blockers

1. **Devnet lifecycle is an incomplete, false-green scaffold.** `onchain/rcx-timepin-v2/scripts/devnet-lifecycle.mjs:52` and `:56` define receiverPd/wormholePd inside a try block but `:101` and `:104` use them outside: a successful preflight reaches ReferenceError. `:99` marks the initialized EvidenceSpec readonly; Rust requires init at `onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lib.rs:226`. `:110–111` uses unaligned wall-clock now+1200, while Rust `lib.rs:605–619` requires the target grid and chain Clock lead. `:78` still uses the synthetic [3;32] feed; `:10` hardcodes Wormhole with a TODO although Rust `lib.rs:571–574` requires the authenticated Receiver-config-selected program. `:62–66`, `:137–146` catch failures without nonzero exit status. Even a successful register/open at `:132–136` proves no capture/final/expire or receipt/WorkPage lifecycle. Needed: valid live source plus linked Loader/config preflight, aligned chain-time target, writable spec, register-or-verify existing immutable spec, full capture/final and expiry paths, independent readback, nonzero failure and machine-readable evidence. Do not run this scaffold as an acceptance gate.

2. **Signerless inspector cannot yet establish P1/P3.** `tools/inspector.mjs:19` guesses cluster from URL, `:34` prints a placeholder context slot, `:36–38` merely displays discriminator bytes. `:42–55` derives ProgramData without validating the actual Program link, owner/variant/size; every authority option byte other than 1 becomes “NONE (Immutable)”. `:66` logs thrown errors without setting a failure exit code. There is no exact Timepin/Core decoding, PDA authentication, candidate/result-hash reconstruction, player/shot discovery, finalized comparison across two independent endpoints, or executable hash comparison. Use genesis identity, real context, strict Loader-v3 decoding, exact final ABI/state validation, decimal-string large integers and structured fail-closed output. Redact a user-supplied RPC URL before logging if it contains local transport credentials.

3. **The old recovery guide and currently exported Core client cannot pass server-off.** `docs/PLAY_WITHOUT_US.md:3–6` labels Core v1 as prototype, then `:28` says everything below works today and `:41–76` points only to v1 crank/shooter. Claims at `:80–98` describe obsolete ring/timing behavior. The successor client `onchain/ratchet-core-g2/client/client-v2.mjs:808–822` exports decoding and forward admission only; it has no reload, activate/settle, reveal, final VOID, forfeit, delegation revocation or work discovery exports. G2 `scripts/` and Timepin-v2 `client/` were empty at inspection. `tools/crank.mjs:23`, `:50–55`, `:74`, `:91` explicitly depend on Ratchet HTTP APIs. Needed: the minimal chain-only terminal/recovery client and account discovery with retained salt recovery; publish commands only after one complete stranger-run lifecycle succeeds. Do not count existing HTTP crank or v1 guide as batch D evidence.

4. **One targeted tracker correction prevents rebuilding the obsolete ABI.** `docs/PERMANENCE_EXECUTION_PLAN.md:625–631` still specifies 182-byte Need / 315-byte Candidate / 229-byte terminal account / 125-byte receipt. Current `onchain/rcx-timepin-v2/README.md:24–51` and `onchain/rcx-timepin/model-v2.mjs:8–32` instead define EvidenceSpec=262, Need=132, Candidate=119, optional 2-record WorkPage (47+106*n, max259); Need itself is the terminal record and there is no TerminalTimepin/CompletionReceipt. Rust `lib.rs:19` already declares canonical C8ww. Old `vectors/register-open-v2.json:4–6` still says US517/nondeployable pending build-owner repin. Register instruction encoding remains hash[32]+canonicalArgs[214], confirmed `lib.rs:57–60` and vectors `:90–108`; this part of the script is not an ABI defect. Update only the existing tracker and generated manifest/vectors after Fable completes its owned candidate.

## Minimal ordered gates in the existing tracker

- **Now, one owner:** finish the atomic C8ww source/Core pin/build/vector/hash candidate and grouped exact-SBF validation. No competing build/test runners or re-use of US517 artifacts. This audit did not execute or independently certify that build.
- **P1/P3 alongside P5 client work:** finish inspector, full no-value Timepin devnet life with actual sponsored Pyth, two unrelated fee payers/runners, and one genuine Ratchet-services-off life; finish smallest crypto G2 reload/play/resolve/recover client. Treat missing oracle/RPC as pause or deterministic expiry/VOID, never a latest-price fallback. Receipt proof must follow the compact WorkPage ABI where sponsored work is exercised.
- **P6 as soon as those paths work:** start the existing 72-hour continuous independent-operation window; collect trace, context, transaction, cost and balance-conservation evidence. Existing `PERMANENCE_EXECUTION_PLAN.md:794–806` explicitly requires it before P7. A shorter drill can find bugs but is not P6. Candidate code changes during the window need an explicit evidence-validity decision, never silent reuse.
- **P7 after evidence and Semir’s deployment approval:** upgradeable opt-in mainnet beta, real production-RCX Token-2022 reload and full selected position via independent client. `PERMANENCE_EXECUTION_PLAN.md:820–836` correctly forbids v2 bounty funding while producers are upgradeable; ordinary fee payers and Core cleanup bond keep execution open. Correct runbook `:1004–1005` if “bounty/reload” is read as requiring that forbidden pilot bounty. No freeze shortcut to make bounty funding pass.
- **P8/P9/P10 retain their existing order and claim boundaries:** prepare legacy snapshot/reconciliation and final recovery bundle independently while the runtime window accrues. Mainnet beta does not establish canonical migration. Full operator-independent cutover needs P8/P9; immutable/trustless completion additionally needs separately approved staged P10 authority removal and real bounty proof, as `:884–900` already specifies.

Defer from the first crypto beta: xStock listing and its separate seven-day cadence gate (`:808–816`), cosmetic website/board work, additional rent/compute micro-optimization after costs are measured, and bounty-enabled permanence beyond the already-required mutable-producer rejection controls. Do not divert into a second canonical off-chain database. This audit proposes no silent waiver of P0–P10 gates.

## Honest verdict

Checked: assigned scripts, selected Rust/model ABI, exact tracker P1/P3/P6/P7/P9/P10 requirements, recovery guide, Core client exports, relevant directory contents. Source-level findings only; ongoing owner edits may supersede the cited snapshot.

Inferred: the listed scaffold faults prevent a successful complete public lifecycle; the currently exposed recovery surface cannot satisfy the stated clean-machine/server-off acceptance. This follows from static code and missing exported paths, not a live failure observation.

Not executed or inspected: cargo, tests, SBF rebuild, devnet/mainnet RPC, signing, key files, env/config secrets, deployments, freeze, migration source data, full Rust economic/security audit. No project source edits, no services stopped, no funds moved. Remote actions used only an agent-owned Svemir scratch reader. Parent owns delivery and native-owner acknowledgement.

## Follow-up: vector writer question (Sol/native is now sole build owner)

No writer has been verified for `onchain/rcx-timepin-v2/vectors/register-open-v2.json` or `lifecycle-v2.json`. A read-only recursive source/script-text search excluding target/node_modules/.git/backups/vendor and env/keypair/secret/config filenames timed out at the Svemir 120-second transport limit without returning its match/count output. Therefore this is NOT proof that a generator is absent and NOT a claim that the vectors are hand-maintained. No further broad scan was run.

Verified narrow evidence: `test/test_timepin_v2_lifecycle_vectors.mjs:3` imports readFileSync; `:22–27` explicitly reads both vector JSONs. Model helpers imported at `:16–18` provide encoders/hashes/PDA derivation; those lines do not write files. The adjacent `onchain/rcx-timepin-v2/svm-tests/scripts/` directory contains only `fetch-mainnet-fixture.mjs`. Root `scripts/` enumeration contains no explicitly Timepin/vector-named generator; filename absence does not exclude an indirectly named writer.

Repin control the sole build owner must account for: `test/test_timepin_v2_lifecycle_vectors.mjs:49–50` currently asserts `deployableIdentity === false` for BOTH vector files. If the finalized C8ww vector metadata changes to true, update these expectations coherently; do not retain a passing assertion whose identity meaning contradicts the release. This follow-up was source/filename inspection only, no tests, generator execution, RPC/signing or project edits. Only this local report was appended. Parent was informed that the broad scan is unverified, not a clean negative result.
