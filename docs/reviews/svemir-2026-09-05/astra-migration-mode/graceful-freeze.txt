PASS  baseline: the machine sells while the freeze is off
PASS  baseline: the shot is open and holding stake
PASS  frozen: a shot is refused
PASS  frozen: refused by NAME, not a generic SHOT_REFUSED — an agent must be able to tell a freeze from a fault, which is exactly what the Bankr RELEASE_MISMATCH failure was
PASS  frozen: the reason says what is happening
PASS  frozen: and says open shots are unaffected, so nobody thinks their stake is stuck
PASS  frozen: a refused seal takes no credits and opens no chamber
PASS  frozen: a challenge is refused too — takeStake is the ONE chokepoint every stake passes, so no second path stays open
PASS  frozen: state still reads
PASS  frozen: the open shot settled rather than sitting there
PASS  frozen: a correct prediction is still recorded as correct
PASS  frozen: and it PAYS — 1.7x the stake returned, exactly as if nothing were frozen
PASS  unset: selling resumes — the switch is off unless it is explicitly on
PASS  RX_MIGRATION_FREEZE=false does NOT freeze — a string comparison against "1", so no truthy accident can take the game down

PASS  migration freeze: 14 checks — it stops selling, it never stops settling
