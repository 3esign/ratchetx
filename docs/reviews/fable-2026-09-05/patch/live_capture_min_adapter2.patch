diff -ruN a/onchain/ratchet-core-g2/programs/ratchet-core-g2/src/foreign_timepin.rs b/onchain/ratchet-core-g2/programs/ratchet-core-g2/src/foreign_timepin.rs
--- a/onchain/ratchet-core-g2/programs/ratchet-core-g2/src/foreign_timepin.rs	2026-09-05 03:19:49.540499875 +0000
+++ b/onchain/ratchet-core-g2/programs/ratchet-core-g2/src/foreign_timepin.rs	2026-09-05 03:20:20.476872539 +0000
@@ -375,22 +375,31 @@
         price_message_hash(record) == record.message_hash,
         TimepinConsumerError::WrongMessageHash
     );
-    require!(
-        record.prev_publish_time < target_ts && target_ts <= record.publish_time,
-        TimepinConsumerError::DoesNotBracketTarget
-    );
-    let pre_gap = target_ts
-        .checked_sub(record.prev_publish_time)
-        .ok_or(TimepinConsumerError::TimestampOverflow)?;
+    if spec.adapter == 2 {
+        // LIVE_CAPTURE_MIN (Timepin ADAPTER_PYTH_PUSH_LIVE_MIN): first sponsored
+        // post at or after the target; the same predicate the producer enforced.
+        require!(
+            target_ts <= record.publish_time,
+            TimepinConsumerError::DoesNotBracketTarget
+        );
+    } else {
+        require!(
+            record.prev_publish_time < target_ts && target_ts <= record.publish_time,
+            TimepinConsumerError::DoesNotBracketTarget
+        );
+        let pre_gap = target_ts
+            .checked_sub(record.prev_publish_time)
+            .ok_or(TimepinConsumerError::TimestampOverflow)?;
+        require!(
+            pre_gap <= i64::from(spec.max_pre_target_gap_seconds),
+            TimepinConsumerError::PreTargetGapTooLarge
+        );
+    }
     let post_lag = record
         .publish_time
         .checked_sub(target_ts)
         .ok_or(TimepinConsumerError::TimestampOverflow)?;
     require!(
-        pre_gap <= i64::from(spec.max_pre_target_gap_seconds),
-        TimepinConsumerError::PreTargetGapTooLarge
-    );
-    require!(
         post_lag <= i64::from(spec.max_post_target_lag_seconds),
         TimepinConsumerError::PostTargetLagTooLarge
     );
@@ -572,7 +581,7 @@
 fn validate_spec_shape(spec: &EvidenceSpecV2View) -> Result<()> {
     require!(
         spec.schema == TIMEPIN_SCHEMA_V2
-            && spec.adapter == 1
+            && (spec.adapter == 1 || spec.adapter == 2)
             && spec.receiver_program != Pubkey::default()
             && spec.push_oracle_program != Pubkey::default()
             && spec.feed_id != [0; 32]
diff -ruN a/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lib.rs b/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lib.rs
--- a/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lib.rs	2026-09-05 03:19:49.538495635 +0000
+++ b/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lib.rs	2026-09-05 03:20:20.476405157 +0000
@@ -21,6 +21,15 @@
 pub const SCHEMA_VERSION: u16 = 2;
 pub const SCHEMA_SEED: [u8; 2] = SCHEMA_VERSION.to_le_bytes();
 pub const ADAPTER_PYTH_PUSH_V2: u8 = 1;
+/// LIVE_CAPTURE_MIN: same sponsored-PDA authentication as adapter 1, but the
+/// admissible message is the first sponsored post at or after the target
+/// (`target_ts <= publish_time <= target_ts + max_post_target_lag`) instead of
+/// the unique Pythnet aggregate bracketing it. Because the sponsored account is
+/// monotone in `publish_time`, the first landed capture is already the minimum;
+/// a later capture can only read an equal or later post, so `capture_conflict`
+/// refuses instead of terminalizing as AMBIGUOUS. See
+/// docs/reviews/fable-2026-09-05/PAYOUT_DETERMINISM_THEOREM.md.
+pub const ADAPTER_PYTH_PUSH_LIVE_MIN: u8 = 2;
 pub const VERIFICATION_FULL: u8 = 1;
 pub const EVIDENCE_SPEC_SEED: &[u8] = b"evidence_spec";
 pub const NEED_SEED: &[u8] = b"need";
@@ -422,7 +431,7 @@
 pub fn validate_spec(args: &EvidenceSpecArgs) -> Result<()> {
     require!(args.schema == SCHEMA_VERSION, TimepinV2Error::WrongSchema);
     require!(
-        args.adapter == ADAPTER_PYTH_PUSH_V2,
+        args.adapter == ADAPTER_PYTH_PUSH_V2 || args.adapter == ADAPTER_PYTH_PUSH_LIVE_MIN,
         TimepinV2Error::WrongAdapter
     );
     require_keys_eq!(
diff -ruN a/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lifecycle.rs b/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lifecycle.rs
--- a/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lifecycle.rs	2026-09-05 03:19:49.538694080 +0000
+++ b/onchain/rcx-timepin-v2/programs/rcx-timepin-v2/src/lifecycle.rs	2026-09-05 03:20:20.476681435 +0000
@@ -15,7 +15,7 @@
 
 use crate::{
     authenticate_generation_accounts, authenticate_need, authenticate_spec_account,
-    validate_need_shape, EvidenceSpecV2, TimepinNeedV2, TimepinV2Error, EVIDENCE_SPEC_SEED,
+    validate_need_shape, EvidenceSpecV2, ADAPTER_PYTH_PUSH_LIVE_MIN, TimepinNeedV2, TimepinV2Error, EVIDENCE_SPEC_SEED,
     NEED_AMBIGUOUS, NEED_CANDIDATE, NEED_EXPIRED, NEED_FINAL, NEED_OPEN, NEED_SEED, SCHEMA_SEED,
     SCHEMA_VERSION,
 };
@@ -940,6 +940,16 @@
         expected_message_hash != first_hash,
         TimepinLifecycleError::DuplicateMustUseFirstCapture
     );
+    // LIVE_CAPTURE_MIN: a different message can only be a LATER sponsored
+    // post. Every landed capture read the account at its own execution slot,
+    // landings are totally ordered by slot, and the account is monotone in
+    // `publish_time`; therefore the first landed capture is the minimum and a
+    // conflicting message is never evidence of ambiguity. Refuse, keep
+    // NEED_CANDIDATE, write nothing.
+    require!(
+        spec.adapter != ADAPTER_PYTH_PUSH_LIVE_MIN,
+        TimepinLifecycleError::LaterThanWinner
+    );
     write_candidate(
         &mut ctx.accounts.candidate_b,
         ctx.bumps.candidate_b,
@@ -1168,23 +1178,35 @@
     need: &TimepinNeedV2,
     candidate: &CandidateV2,
 ) -> Result<()> {
-    require!(
-        candidate.prev_publish_time < need.target_ts && need.target_ts <= candidate.publish_time,
-        TimepinLifecycleError::DoesNotBracketTarget
-    );
-    let pre_gap = need
-        .target_ts
-        .checked_sub(candidate.prev_publish_time)
-        .ok_or(TimepinLifecycleError::TimestampOverflow)?;
+    if spec.adapter == ADAPTER_PYTH_PUSH_LIVE_MIN {
+        // LIVE_CAPTURE_MIN: the first sponsored post at or after the target.
+        // `prev_publish_time` is not a decision field here; the sponsored
+        // account never exposes the bracketing aggregate on a fixed-phase
+        // pusher (measured 0/25 minute targets on 2026-09-05).
+        require!(
+            need.target_ts <= candidate.publish_time,
+            TimepinLifecycleError::DoesNotBracketTarget
+        );
+    } else {
+        require!(
+            candidate.prev_publish_time < need.target_ts
+                && need.target_ts <= candidate.publish_time,
+            TimepinLifecycleError::DoesNotBracketTarget
+        );
+        let pre_gap = need
+            .target_ts
+            .checked_sub(candidate.prev_publish_time)
+            .ok_or(TimepinLifecycleError::TimestampOverflow)?;
+        require!(
+            pre_gap <= i64::from(spec.max_pre_target_gap_seconds),
+            TimepinLifecycleError::PreTargetGapTooLarge
+        );
+    }
     let post_lag = candidate
         .publish_time
         .checked_sub(need.target_ts)
         .ok_or(TimepinLifecycleError::TimestampOverflow)?;
     require!(
-        pre_gap <= i64::from(spec.max_pre_target_gap_seconds),
-        TimepinLifecycleError::PreTargetGapTooLarge
-    );
-    require!(
         post_lag <= i64::from(spec.max_post_target_lag_seconds),
         TimepinLifecycleError::PostTargetLagTooLarge
     );
@@ -1498,6 +1520,8 @@
     UseConflictInstruction,
     #[msg("duplicate message must use capture_first")]
     DuplicateMustUseFirstCapture,
+    #[msg("LIVE_CAPTURE_MIN: a later sponsored post never replaces the first landed capture")]
+    LaterThanWinner,
     #[msg("one Candidate is required")]
     CandidateRequired,
     #[msg("an unanswered Open Need is required")]
