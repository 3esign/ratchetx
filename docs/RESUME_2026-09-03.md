# Resume point — 2026-09-03, 03:30

Status: **work is stopped and preserved, not finished.** This file is the first
thing a continuing session should read. It records what is proven, what is
half-built, and what must not be re-litigated. It authorizes nothing.

## 1. Where the code actually is

| Layer | State |
| --- | --- |
| `origin/main` | `dc65065` — h113, keyless feeds enforced in `lib/prices.js` |
| local `main` | 3 commits ahead and **unpushed**: `848ab24` (Pyth source-time binding + real Token-2022 + `client/inspect.mjs` + execution plan), `8463f95` (pinned artifact `ratchet_core-v1-2026-09-03.so`, 414,344 bytes, hash in `docs/CORE.md`), `27fbace` (`tools/permanence-release.mjs` + manifest example + test) |
| this commit | the unverified Supabase-retirement and Timepin work below, preserved so it cannot be lost |

Pushing is the founder's: the VM has no git credentials and the cloud proxy
refuses this repository.

## 2. Production is down, and it is not the code

`/api/game?action=board`, `action=feeds` and `/api/log` return **500**.
`/api/supply` leaks the cause in its own error text: **`exceed_egress_quota`** —
Supabase is refusing reads because this cycle's egress allowance is spent. The
static site still serves; no state is lost; this is a read outage.

**First action on resume, before anything else:** the Supabase usage screen —
how much egress was consumed, and the date the cycle resets. That one screen
decides between "wait for the reset" and "we have an egress problem to
engineer". Do not guess which read dominates. Unmeasured candidates, ranked:
the 10-second state poll's full player row; the settlement watch re-walking
hour buckets on every retry while a shot expires; the sample buckets, which are
read-modify-written whole once a minute.

Note the two different 402s, per `SUPABASE_RETIREMENT_PLAN.md`: Supabase's
`exceed_egress_quota` is an outage and a migration blocker; RatchetX's own x402
`Payment Required` is a deliberate protocol response. They are not related.

## 3. The unverified packet in this commit — do not treat it as done

| Path | Lines | State |
| --- | --- | --- |
| `docs/SUPABASE_RETIREMENT_PLAN.md` | 743 | The cutover contract: move authority before deleting storage. Not executed. |
| `docs/SUPABASE_FINAL_SNAPSHOT.md` | 120 | The recovery/snapshot procedure. No snapshot has been taken; no `releases/legacy-snapshot-manifest-*.json` exists. |
| `tools/supabase_final_snapshot.mjs` | 1,250 | **Half-built.** See the three failures below. |
| `test/test_supabase_final_snapshot.mjs` | 118 | **3 of 6 checks fail** — this is the resume point, not a regression. |
| `test/test_supabase_final_snapshot_restore.mjs` | 88 | passes |
| `onchain/rcx-timepin/` | program + `SPEC.md` + LiteSVM tests + CI workflow | Prototype. No value, no deploy. |
| `docs/PERMANENCE_EXECUTION_PLAN.md` | +126 / −42 | The active map, edited and uncommitted when work stopped. |

The three failing checks, verbatim:

```
not ok 3 - ceremony arguments require a writer barrier and evidence hash   error: UNKNOWN_ARGUMENT
not ok 4 - conservation validation rejects malformed or negative buckets   error: INVALID_SETTLEMENT_OUTBOX_STAKE
not ok 6 - public manifest gate rejects values and credentials             error: Cannot read properties of undefined (reading 'sha256')
```

They are the tool and its test disagreeing: an argument the tool does not accept
yet, a validation code that does not match, and a manifest field the tool never
writes. **The release gate is red until these pass. That is on purpose** — the
same convention the repo already uses for the artifact check. A red `npm test`
does not block a Vercel deploy: Vercel's build command is
`node lib/check_store_schema.js`, not the test suite.

## 4. Settled. Do not re-open these.

1. **No API keys, ever.** Every canonical path — seal, checkpoint, settlement,
   void/refund, withdrawal, proof, recovery, independent client — must complete
   with every operator-held key variable absent. A keyed integration is optional
   and display-only, or off. A private RPC is a transport choice, never protocol
   authority. h113 enforces this in code: recognising a ticker is kept for an
   honest refusal, but no secret can make a new economic target appear.
2. **Stocks are held, not cancelled.** Hermes needs a credential; the free
   tokenized rail publishes on ~870s against a 60s seal bound (~7% of stock
   seals would land). Both measurements are on record. The equity-pull candidate
   is retired (`b087926`).
3. **Solana is the canonical database.** A website, indexer or cache may improve
   access; none may create, erase or reinterpret balances, positions, evidence
   or outcomes.
4. **No freeze is scheduled.** 2026-09-08 is not an execution deadline;
   `docs/FREEZE.md` is historical. An immutability ceremony needs fresh explicit
   authorization after the system has earned it. Seal v2's own ceremony, if it
   passes its own checklist, is a separate thing and is not permission to freeze
   Core.
5. **The settlement package is approved and homeless by design.** Decision band,
   horizons by asset class, PUMP off the short clock. Horizons and PUMP are
   board policy — reversible presentation, server-side, no build. The band is
   evidence-gathering: **no `k` may be frozen**, because the current on-chain
   layout does not retain enough confidence evidence to implement or audit one
   (execution-plan blocker 3). A read-only replay at k=1 already reads BTC-5m
   11.1% (5/45), ETH-5m 23.8% (5/21), SOL-5m 2.9% (1/34), overall 10.2%
   (18/177) — smoke-agent-heavy and correlated, so it freezes nothing, but it is
   enough to reject the model as reassuring on its own.
6. **Corrections that must not be re-learned.** There is no CFTC guidance dated
   2026-09-01. Coinbase has never been able to settle a shot: `priceCrossing`
   filters to `pyth-onchain`, and `test_settle.mjs` §5b has asserted it for
   weeks. The confidence gate (`MAX_CONF_BPS = 200`), the multi-RPC ladder and
   the Brier sample-size gate all already existed.

## 5. Order of work on resume

1. Read the Supabase usage screen. Everything below waits on it.
2. Push the three local commits so CI can see them.
3. Fix the three failing checks in `tools/supabase_final_snapshot.mjs`, then take
   the final snapshot the retirement plan requires. No migration root may be
   guessed from a quota-restricted read.
4. Board policy (horizons + PUMP) — one server-side commit, no build, reversible.
5. Shadow the decision band and let it run; it now has no deadline to serve.
6. Then the execution plan's P0→P10, in its own order.
7. Bankr, once the API answers: tell it `update the ratchetx skill from
   github.com/3esign/ratchetx` (it still runs 1.5.0; the repo has 1.6.0), then
   the two live probes — one that must play the shortest target rather than
   Coinbase, one that must resolve a misspelt Tesla and refuse it by name.

## 6. Traps that cost time before

- Run `test_session_play_intent.mjs` from inside `test/`; it spawns the runner by
  a relative path.
- `git` on this mount cannot unlink: sweep `.git/*.lock` after every write, and
  commit with `write-tree` / `commit-tree` when porcelain times out.
- Never `git add .` — the repo root carries ~58 untracked scratch files.
- Never `git stash -u`.
- Measure, then claim. Of the three problems reported on 2026-09-02, two were
  faults in the instrument rather than the machine.
